[project]
name = "product-service"
version = "0.0.1"
description = "Microservice for managing orders and communicating with product service"
authors = [
    {name = "Alabaykin",email = "bells.vtv@gmail.com"}
]
license = "MIT"
readme = "README.md"
requires-python = "^3.12"
dependencies = [
    "fastapi (>=0.136.1,<0.137.0)",
    "uvicorn[standart] (>=0.46.0,<0.47.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
