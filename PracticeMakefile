create-structure:
	mkdir -p src tests docs
	touch README.md setup.py requirements.txt docs/DOMAIN.md src/.gitkeep tests/.gitkeep

install-dep:
	pip install -r requirements.txt

install:
	pip install -e .
	
test-pytest:
	python3 -m pytest